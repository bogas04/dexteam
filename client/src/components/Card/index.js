import styled from "react-emotion";
import colors from "../../design/colors";

const Card = styled.div`
  margin: 15px 0;
  border-radius: 5px;
  box-shadow: 0 0 5px 2px ${colors.subtleGrey};
  padding: 10px;
`;

export default Card;
